"""Sync event selection (placeholder).

Planned responsibilities:
- Choose sync timestamps (movement start, first bottom, first lockout) from detected events.
- Provide offsets that the FFmpeg compositor can consume.
"""

# TODO: Implement sync event selection helpers.
